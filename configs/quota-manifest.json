{
  "_comment": "Daily quota SSOT for content production. Numbers are from live measurement / official API. Read by history_batch.py and quota.sh. To change, edit this file only.",
  "_scope": "Content one-person media automation only. Care (Tailscale · care daemon) is out of scope.",
  "tistory": {
    "limit_per_day": 15,
    "unit": "new posts / account (shared across 5 blogs)",
    "source": "live 2026-08-15: galaxys21 13 + mynote 2 = 15, 16th → 403",
    "measured_by": "RSS today pubDate count (history_batch.today_published_count)"
  },
  "youtube": {
    "units_per_day": 10000,
    "units_per_upload": 1600,
    "approx_uploads_per_day": 6,
    "unit": "Data API v3 quota units",
    "source": "YouTube Data API v3 default allocation (videos.insert=1600 units)",
    "measured_by": "no local upload counter → exact remaining only visible in Cloud Console"
  },
  "threads": {
    "limit_per_day": 250,
    "chars_per_post": 500,
    "unit": "API-published posts / day (per profile)",
    "source": "Meta Threads API official (comments not included in the 250 limit)",
    "measured_by": "manual (not wired)"
  },
  "naver": {
    "limit_per_day": null,
    "note": "no write API → manual paste, no hard limit",
    "measured_by": "manual"
  }
}
