# 🤖 자동화 파이프라인 — 아이디어만 던지면 다 만들어지는 구조

> 이 폰(Galaxy Z Fold4)은 "아이디어 → 글 → 영상 → 발행"까지 전부 자동화돼 있다.
> 너는 **아이디어만 말하면 된다.** 나머지는 Claude Code가 알아서 한다.

---

## 핵심 원리 (한 줄)

**"텍스트나 아이디어 하나만 던지면, 파이프라인이 글 → 웹페이지 → 숏폼 영상 → 티스토리/유튜브 발행까지 다 해준다."**

---

## 전체 흐름 (BOM 순서)

```
아이디어(한 줄)
   ↓
① 글 작성       → 마크다운(.md)으로 정리
   ↓
② 웹페이지      → build_webzine.py 가 HTML로 변환
   ↓
③ 숏폼 영상     → produce_pd.sh 가 페이지 읽고 영상 제작
   ↓
④ 티스토리 발행  → tistory_sync.sh (업무일지 자동 발행)
   ↓
⑤ 유튜브 업로드  → yt_upload.py (OAuth 연결돼 있음)
   ↓
⑥ 네이버 확산    → naver_recipe.py (붙여넣기 템플릿)
```

---

## 각 단계별 — 뭘 던지면 뭘 해주나

### ① 글 작성
**던지는 것:** "족발집 운영 팁 하나 알려줘"
**되는 것:** Claude Code가 마크다운 글로 정리 → `_notebook/`에 저장

### ② 웹페이지
**명령어:** `python3 scripts/build_webzine.py`
**되는 것:** 마크다운 → 예쁜 HTML 웹페이지 자동 생성 (검색·접기·펼치기 UI 포함)

### ③ 숏폼 영상
**명령어:** `bash scripts/produce_pd.sh [이름] "[URL]"`
**되는 것:** 웹페이지를 읽고 → 섹션별 캡처 → 유진 목소리 내레이션 → 자막 → BGM → mp4

### ④ 티스토리 발행
**명령어:** `bash scripts/tistory_sync.sh`
**되는 것:** 업무일지를 티스토리 블로그에 자동 발행 (카카오 세션 연결됨)

### ⑤ 유튜브 업로드
**명령어:** `python3 scripts/yt_upload.py --title "제목" --file 영상.mp4 --privacy public`
**되는 것:** 마왕가족(@남성훈-f7i) 채널에 자동 업로드

### ⑥ 네이버 확산
**명령어:** `python3 scripts/naver_recipe.py`
**되는 것:** 네이버 블로그에 붙여넣을 원고 + 태그 템플릿 자동 생성

---

## 자주 쓰는 "아이디어 → 결과" 예시

### 예시 1: 족발 매장 홍보 숏폼
```
너: "족발 매장 홍보 숏폼 만들어줘"
→ ① 글 작성 (족발 맛, 매장 정보)
→ ② 웹페이지 (족발 랜딩)
→ ③ 숏폼 영상 (produce_pd.sh)
→ ⑤ 유튜브 업로드
= 끝. 영상이 유튜브에 올라감
```

### 예시 2: 치킨 운영 노하우 글
```
너: "치킨 바삭하게 튀기는 법 알려줘"
→ ① 글 작성
→ ② 웹페이지
→ ④ 티스토리 발행
= 끝. 티스토리 블로그에 글이 올라감
```

---

## 명령어 한 번에 정리

| 하고 싶은 것 | 명령어 |
|---|---|
| 글 → 웹페이지 | `python3 scripts/build_webzine.py` |
| 웹페이지 → 숏폼 영상 | `bash scripts/produce_pd.sh 이름 "URL"` |
| 티스토리 발행 | `bash scripts/tistory_sync.sh` |
| 유튜브 업로드 | `python3 scripts/yt_upload.py --title "제목" --file 영상.mp4` |
| 네이버 원고 | `python3 scripts/naver_recipe.py` |

---

## 정리 — 이 시스템의 진짜 힘

**너는 "아이디어"만 던지면 된다.**

- 글쓰기? → Claude Code가 한다
- 웹페이지? → build_webzine.py 가 한다
- 숏폼 영상? → produce_pd.sh 가 한다
- 티스토리/유튜브 발행? → 자동화 스크립트가 한다

**너가 하는 일은 딱 하나: "이거 만들어줘"라고 말하는 것.**

---

*자동화 파이프라인 설명 · Claude Code · 2026-08-19*
