name: 📰 Tistory RSS → 기자/ (galaxys21-pwuser)

# 허브(bayaba_phone) 자신의 티스토리 동기화 — 중앙 재사용 워크플로 호출.
on:
  schedule:
    - cron: '7 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: bayaba-1979/bayaba_phone/.github/workflows/tistory-sync.yml@main
    with:
      blog: galaxys21-pwuser
